variants = [15 * 3, 15 / 3, 15 // 2, 15 ** 2, True, [1, 2, 3]]
for i in range(len(variants)):
    print(type(variants[i]))

# Создаем список и прогоняем каждый его элемент церез цикл, выводим на экран результат тип каждого элемента внутри списка
# Добавил несколько своих элементов для закрепления